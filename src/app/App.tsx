import ComponentGallery from '@/app/ComponentGallery'

/** Placeholder shell. Track 3 replaces this with the router; until then it
 *  renders the component gallery so the library can be judged in a browser. */
export default function App() {
  return <ComponentGallery />
}
